# ============================================
# Attestation Service - TESTNET Configuration
# ============================================
# For running alongside mainnet on a different port.
# Copy to .env.testnet and fill in your values:
#   cp env.testnet.example .env.testnet

# Witness private key (same key, but MUST be authorized on testnet PaymentVerifier)
WITNESS_PRIVATE_KEY=0x_YOUR_WITNESS_PRIVATE_KEY_HERE

# Base Sepolia (chain 84532)
CHAIN_ID=84532
VERIFIER_CONTRACT=0xd72ddbFAfFc390947CB6fE26afCA8b054abF21fe
OFFRAMP_CONTRACT=0x34249F4AB741F0661A38651A08213DDe1469b60f
RPC_URL=https://base-sepolia-rpc.publicnode.com

# Testnet instance port (mainnet uses 4001)
PORT=4002

# Qonto sandbox domain for TLSNotary proof validation
ALLOWED_SERVERS=thirdparty-sandbox.staging.qonto.co

# Solver API keys (same format: key1:0xAddr1,key2:0xAddr2)
# SOLVER_API_KEYS=

# Logging
RUST_LOG=info

# Audit log (separate from mainnet)
# AUDIT_LOG_PATH=/var/log/attestation-testnet.log
