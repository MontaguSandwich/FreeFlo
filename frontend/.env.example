# ===========================================
# Frontend Environment Variables
# ===========================================
# Copy this file to .env.local and fill in your values

# Network selection: "mainnet" (Base, chain 8453) or "testnet" (Base Sepolia, chain 84532)
# Controls contract addresses, default chain, and RPC endpoints automatically.
# Default: mainnet
NEXT_PUBLIC_NETWORK=mainnet

# WalletConnect Project ID
# Get one at https://cloud.walletconnect.com/
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=

# RPC URL for reading blockchain data (optional, uses public RPC if not set)
NEXT_PUBLIC_RPC_URL=https://base-sepolia.g.alchemy.com/v2/YOUR_API_KEY

# Use mock quotes for testing (set to 'true' to use mock data instead of on-chain quotes)
# Default: false (uses real on-chain quotes)
NEXT_PUBLIC_USE_MOCK_QUOTES=false

# Solver Quote API URL (server-side, used by the /api/quote proxy)
# For production, set this to your solver's URL (e.g., http://95.217.235.164:8081)
SOLVER_API_URL=http://127.0.0.1:8081
