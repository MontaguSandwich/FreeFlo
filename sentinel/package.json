{
  "name": "freeflo-sentinel",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "FreeFlo Sentinel — error detection, alerting, and (non-refund) self-healing keeper.",
  "scripts": {
    "sweep": "tsx src/index.ts --once",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0"
  }
}
