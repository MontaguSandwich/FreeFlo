# FreeFlo Attestation Service - Environment Configuration
# Copy to /etc/freeflo/attestation.env and fill in values
# IMPORTANT: This file contains secrets - chmod 600 and restrict access

# =============================================================================
# REQUIRED: Witness signing key
# =============================================================================
# The private key used to sign EIP-712 attestations
# Generate with: cast wallet new
# NEVER share this key - it controls attestation signing
WITNESS_PRIVATE_KEY=0x_YOUR_WITNESS_PRIVATE_KEY_HERE

# =============================================================================
# Chain configuration
# =============================================================================
CHAIN_ID=84532
VERIFIER_CONTRACT=0xd72ddbFAfFc390947CB6fE26afCA8b054abF21fe

# =============================================================================
# TLS proof validation
# =============================================================================
# Allowed TLS server hostnames (comma-separated)
ALLOWED_SERVERS=thirdparty.qonto.com

# =============================================================================
# Service configuration
# =============================================================================
# Port to listen on (behind reverse proxy)
PORT=4001

# Rust logging level
RUST_LOG=info,attestation_service=debug

# =============================================================================
# Solver Authentication (Phase 2)
# =============================================================================
# API keys for authorized solvers
# Format: key1:0xSolverAddr1,key2:0xSolverAddr2
# Generate keys with: openssl rand -hex 32
# If not set, authentication is DISABLED (not recommended for production)
SOLVER_API_KEYS=

# Rate limit per solver per minute (default: 100)
RATE_LIMIT_PER_MINUTE=100

# =============================================================================
# On-Chain Intent Validation (Phase 2)
# =============================================================================
# RPC URL for Base Sepolia
# If not set, on-chain validation is DISABLED
RPC_URL=https://base-sepolia-rpc.publicnode.com

# OffRampV3 contract address
OFFRAMP_CONTRACT=0x34249F4AB741F0661A38651A08213DDe1469b60f

# =============================================================================
# Audit Logging (Phase 2)
# =============================================================================
# Path to audit log file (JSON lines format)
# If not set, logs only to stdout via tracing
AUDIT_LOG_PATH=/var/log/freeflo/attestation-audit.log
