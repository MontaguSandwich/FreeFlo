events {
    worker_connections 1024;
}

http {
    # Logging
    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log;

    # Rate limiting
    limit_req_zone $binary_remote_addr zone=api:10m rate=10r/s;
    limit_req_zone $binary_remote_addr zone=quote:10m rate=30r/m;

    # Upstream services
    upstream solver_health {
        server solver:8080;
    }

    upstream solver_api {
        server solver:8081;
    }

    upstream attestation {
        server attestation-service:4001;
    }

    # HTTP server (redirect to HTTPS or serve directly)
    server {
        listen 80;
        server_name _;

        # Health check endpoint (no rate limit)
        location /health {
            proxy_pass http://solver_health/health;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }

        # Quote API
        location /api/quote {
            # Rate limit: 30 requests per minute per IP
            limit_req zone=quote burst=5 nodelay;

            # CORS headers
            add_header 'Access-Control-Allow-Origin' '*' always;
            add_header 'Access-Control-Allow-Methods' 'GET, OPTIONS' always;
            add_header 'Access-Control-Allow-Headers' 'Content-Type' always;

            if ($request_method = 'OPTIONS') {
                add_header 'Access-Control-Allow-Origin' '*';
                add_header 'Access-Control-Allow-Methods' 'GET, OPTIONS';
                add_header 'Access-Control-Allow-Headers' 'Content-Type';
                add_header 'Access-Control-Max-Age' 1728000;
                add_header 'Content-Type' 'text/plain; charset=utf-8';
                add_header 'Content-Length' 0;
                return 204;
            }

            proxy_pass http://solver_api/api/quote;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        }

        # Supported currencies/RTPNs
        location /api/supported {
            limit_req zone=api burst=10 nodelay;

            add_header 'Access-Control-Allow-Origin' '*' always;

            proxy_pass http://solver_api/api/supported;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }

        # Stats endpoint (internal only in production)
        location /stats {
            # Uncomment to restrict to internal IPs
            # allow 10.0.0.0/8;
            # allow 172.16.0.0/12;
            # allow 192.168.0.0/16;
            # deny all;

            proxy_pass http://solver_health/stats;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }

        # Attestation endpoints (internal - restrict in production)
        location /attestation/health {
            proxy_pass http://attestation/health;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }

        # Attestation API (internal only - restrict to solver container)
        location /attestation/attest {
            # In production, restrict to internal IPs only
            # allow 172.16.0.0/12;
            # deny all;

            limit_req zone=api burst=5 nodelay;

            proxy_pass http://attestation/attest;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header Content-Type application/json;
            proxy_read_timeout 120s;  # Attestation can take time
        }

        # Default: 404
        location / {
            return 404 '{"error": "Not found"}';
            add_header Content-Type application/json;
        }
    }

    # HTTPS server (uncomment when you have SSL certs)
    # server {
    #     listen 443 ssl http2;
    #     server_name your-domain.com;
    #
    #     ssl_certificate /etc/nginx/ssl/fullchain.pem;
    #     ssl_certificate_key /etc/nginx/ssl/privkey.pem;
    #
    #     # Include same location blocks as above
    # }
}

