# ===========================================
# Solver Environment Variables
# ===========================================
# Copy this file to .env and fill in your values

# ===========================================
# Required: Chain Configuration
# ===========================================

# RPC URL for Base Sepolia (or mainnet for production)
RPC_URL=https://base-sepolia.g.alchemy.com/v2/YOUR_API_KEY

# Chain ID (84532 for Base Sepolia, 8453 for Base Mainnet)
CHAIN_ID=84532

# ===========================================
# Required: Contract Configuration
# ===========================================

# OffRampV2 contract address
OFFRAMP_V2_ADDRESS=0x...

# ===========================================
# Required: Solver Wallet
# ===========================================

# Solver private key (with 0x prefix)
# This account must be registered as a solver on the contract
SOLVER_PRIVATE_KEY=0x...

# ===========================================
# Optional: Orchestrator Settings
# ===========================================

# How often to poll for new intents/quotes (milliseconds)
POLL_INTERVAL=5000

# Minimum USDC amount to process (in smallest units, 6 decimals)
# 1000000 = 1 USDC
MIN_USDC_AMOUNT=1000000

# Maximum USDC amount to process (in smallest units, 6 decimals)
# 10000000000 = 10,000 USDC
MAX_USDC_AMOUNT=10000000000

# ===========================================
# Optional: Database
# ===========================================

# SQLite database path
DB_PATH=./solver.db

# ===========================================
# Optional: Health Check Server
# ===========================================

# Port for health check HTTP server
# Endpoints: /health, /ready, /live, /stats
HEALTH_PORT=8080

# ===========================================
# PROVIDER CONFIGURATIONS
# ===========================================
# Add provider-specific environment variables here as you integrate them.
# Each provider should have at minimum an ENABLED flag.
#
# Example for Qonto (when ready):
#
# QONTO_ENABLED=false
# QONTO_API_KEY=
# QONTO_SECRET_KEY=
# QONTO_IBAN=
#
# ===========================================
